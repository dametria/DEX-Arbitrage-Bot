{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "rm -f yarn.lock || true",
    "build": "npm install && npm run typecheck && npm run build:workspaces",
    "build:workspaces": "cd artifacts/api-server && npm run build && cd ../mockup-sandbox && npm run build && cd ../mobile && npm run build",
    "typecheck": "tsc --build"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
